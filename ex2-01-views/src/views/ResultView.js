import View from "../views/View";
export default class ResultView extends View {
  constructor(el) {
    super(el);
  }

  mount(list = []) {
    let html = "";

    document.create;

    list;
    debugger;

    this.el.innerHtml = html;
  }
}
